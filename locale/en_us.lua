ADDON:ImportAPI(API.X2Locale)

if locale == nil then
  locale = {}
end

locale.addon = {
  name      = "UI Manager",
  title     = "UI Manager",
  save      = "Save",
  guideText = "Enter a UI name.",
  load      = "Load",
}

-- function X2Locale:GetLocale()
  -- return "fr"
  -- return "ru"
  -- return "zh_cn"
-- end
