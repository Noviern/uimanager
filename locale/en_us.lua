ADDON:ImportAPI(API.X2Locale)

if locale == nil then
  locale = {}
end

locale.addon = {
  name      = "UI Manager",
  title     = "UI Manager",
  save      = "Save",
  guideText = "Enter a UI name.",
  load      = "Load",
}
